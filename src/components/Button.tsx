import React from "react";

const CustomButton = () => {
  return (
    <button className="w-3/5 max-w-xs bg-primary text-white font-medium py-3 px-8 rounded-full shadow-lg transition hover:bg-primary text-xs tracking-wider">
      VIEW ALL
    </button>
  );
};

export default CustomButton;
